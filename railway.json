{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "export PUPPETEER_CACHE_DIR=.cache/puppeteer; npm install && cd client && npm install && npm run build && cd ../server && npm install && npx --yes puppeteer browsers install chrome"
  },
  "deploy": {
    "startCommand": "cd server && npm start"
  }
}
