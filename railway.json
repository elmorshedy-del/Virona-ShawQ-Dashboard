{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "PUPPETEER_CACHE_DIR=.cache/puppeteer npm install && cd client && npm install && npm run build && cd ../server && PUPPETEER_CACHE_DIR=.cache/puppeteer npm install && PUPPETEER_CACHE_DIR=.cache/puppeteer npx --yes puppeteer browsers install chrome"
  },
  "deploy": {
    "startCommand": "cd server && npm start"
  }
}
