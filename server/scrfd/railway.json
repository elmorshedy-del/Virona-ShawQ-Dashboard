{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "pip uninstall -y opencv-python 2>/dev/null; python face_detection_server.py",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
