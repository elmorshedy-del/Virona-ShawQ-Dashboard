This folder preserves the old training-era code and small tracking manifests that were replaced by the pre-trained Muaalem integration.

Preserved here:
- old classifier and bootstrap code
- old training entrypoints
- small JSON manifests and legacy embedding snapshots

Not preserved here:
- large generated augmentation folders
- large classifier checkpoints

Those large assets were removed from the active app tree to keep the new integration lightweight.
