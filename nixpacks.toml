[phases.setup]
# Install Node.js 20 so npm is available, plus your existing tools
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake", "yt-dlp", "ffmpeg"]

[phases.install]
cmds = ["npm install"]

[phases.install.env]
PYTHON = "python3"

[phases.build]
cmds = []

[start]
cmd = "npm start"
