[phases.setup]
# Include Node.js so npm is available during install and build stages
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install.env]
PYTHON = "python3"

[phases.build.env]
PYTHON = "python3"
