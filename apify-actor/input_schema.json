{
  "title": "Facebook Ads Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchQuery": {
      "title": "Search Query",
      "type": "string",
      "description": "Brand or keyword to search for",
      "editor": "textfield"
    },
    "country": {
      "title": "Country",
      "type": "string",
      "description": "Country code (e.g., US, GB, ALL)",
      "default": "US",
      "editor": "textfield"
    },
    "limit": {
      "title": "Max Results",
      "type": "integer",
      "description": "Maximum number of ads to return",
      "default": 10,
      "minimum": 1,
      "maximum": 100
    },
    "filterResults": {
      "title": "Filter Results",
      "type": "boolean",
      "description": "Only return ads matching the search query",
      "default": true
    }
  },
  "required": ["searchQuery"]
}
